run:
  timeout: 1m

linters:
  enable:
    - lll
    - gofmt
    - revive
    - exportloopref
    - unparam

issues:
  exclude:
    - "unexported-return"

linters-settings:
  lll:
    line-length: 180
